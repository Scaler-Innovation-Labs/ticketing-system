
export function calculateTAT(ticket: any, context?: any) {
    return { deadline: null, isOverdue: false, formattedDeadline: 'N/A', expectedResolution: null };
}
export const calculateTATInfo = calculateTAT;
